export const CAMPAIGN_STATUSES = ["pending", "approved", "rejected", "suspended"] as const;
export const CONTRIBUTION_STATUSES = ["pending", "approved", "rejected"] as const;
export const WITHDRAWAL_STATUSES = ["pending", "approved", "rejected"] as const;
